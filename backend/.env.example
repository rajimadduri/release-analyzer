# --- Interview / demo mode (no API credits needed) ---
CLAUDE_API_KEY=demo
DEMO_MODE=true

# --- Live Claude API (comment out demo lines above, uncomment below) ---
# CLAUDE_API_KEY=sk-ant-your-actual-key-here
# DEMO_MODE=false
# DEMO_FALLBACK=true

# Optional: if live API fails (billing/network), return sample results instead of error
# DEMO_FALLBACK=true
